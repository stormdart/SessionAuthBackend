services:
  postgres:
    image: postgres:18
    container_name: session-auth-postgres
    environment:
      POSTGRES_DB: session_auth
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5454:5432"
    volumes:
      - session_auth_data:/var/lib/postgresql

volumes:
  session_auth_data: